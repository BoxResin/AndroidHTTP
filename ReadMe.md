# AndroidHTTP

```Java
HttpRequest request = new HttpRequest()
					.addHeader("id", "something")
					.addHeader("passwd", "something")
					.addParameter("num", "10");
			
HttpLauncher.launch(request);
```
